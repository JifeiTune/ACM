    一、判环问题
    环本质上是个强连通分量，所以Tarjan当然可以判断
    无向图的环很好判断，因为边都是双向的，并查集、dfs、bfs（能搜索到已遍历的点）都行
    有向图麻烦些。dfs可以判断，但条件不再是搜索到已经遍历过的点，而是搜索到的点，当前还在dfs栈内。拓扑排序也行，如果dfs的方法进行拓扑排序，方法同前，如果用取点删边法，则在最后输出节点数少于总节点数代表有环，因为环内节点始终入度不为0，不会被加入排序。
